{
  "name": "portfolio3.0",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "deploy": "cd infra && pulumi up",
    "frontend": "cd frontend && npm start",
    "build": "./scripts/build.sh",
    "build:frontend": "cd frontend && npm run build",
    "build:getBlob": "cd functions/getBlob && npm run build",
    "bootstrap": "./scripts/bootstrap.sh"
  }
}
